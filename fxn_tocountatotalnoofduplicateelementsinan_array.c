#include <stdio.h>
int x;
int duples(int a[], int x);
int main()
{
    int n;
    printf("enter the size of array ");
    scanf("%d", &n);
    int a[n];
    printf("enter the values ");
    for (int i = 0; i < n; i++)
        scanf("%d", &a[i]);
   printf("total no. of duplicate elements: ",duples(a, n));
    return 0;
}
int duples(int a[], int n)
{ int c;
    for (int i=0;i<n;i++)
    
    
    
    
    return c;

}